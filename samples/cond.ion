// special if (switch/case)
foo = 10
cond (foo)
  ==(10) put('1')
  else put('2')

cond (foo)
  ==(8)     put('?')
  equals(9) put('It is 9!')
  else      put('It is not 9!')

// inside lambda
cycle = (age) =>
  cond (age)
    >(13) 'child'
    else  'old'

put(cycle(12))
put(cycle(13))
